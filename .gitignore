# Godot 4 project files
.godot/
/android/
/build/
/export/
*.tmp

# Exported binaries
*.apk
*.aab
*.exe
*.pck
*.zip

# Editor / OS noise
.import/
# export_presets.cfg is NOT ignored. It holds no secrets - the Android keystore is an
# editor setting rather than a preset field - and the Pages workflow cannot export the
# web build without it.
.DS_Store
Thumbs.db

# Python bytecode from the tools
__pycache__/
*.pyc
